name: CI

on:
  push:
    branches: [ master ]

    workflow_dispatch:
    
jobs:
    display:
      runs-on: ubuntu-latest
      env:
        TOKEN: OTY5Mjc1MzY5NTg2ODM1NDc2.YmrCCA.86R6_8SXYLpt9jRG__XhBK21sNc

      steps:
        - name: Display custom environment variables
          run: |
            echo "le token est ".${{ env.TOKEN }}
